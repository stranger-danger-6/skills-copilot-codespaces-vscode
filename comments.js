// Create web server
// Create a web server that listens to the port 3000. 
// It should read the comments.json file and return the comments in the file when a GET request is made to the path /comments.

// The comments.json file contains an array of comments. Each comment has a title and a message.

// Create a new file, comments.js, and add the following code:

// const http = require('http');
// const fs = require('fs');

// const hostname = '